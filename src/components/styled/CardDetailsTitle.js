import styled from "styled-components";
import CheckoutFormTitle from "./CheckoutFormTitle";

const CardDetailsTitle = styled(CheckoutFormTitle)`
  font-weight: bold;
`;

export default CardDetailsTitle;
